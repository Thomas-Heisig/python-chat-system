name: CodeQL

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    name: Default Setup Status
    runs-on: ubuntu-latest

    steps:
      - name: Info
        run: echo "CodeQL scanning is managed by GitHub default setup (dynamic workflow)."
